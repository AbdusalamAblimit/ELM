server:
  port: 10400
spring:
  cloud:
    config:
      name: elm-business-server-1
      profile: dev
      label: elm-configs
      discovery:
        enabled: true
        service-id: elm-config-server

eurekahost1: 8.130.64.168
eurekahost2: 8.130.54.125

  
eureka:
  client:
    service-url:
      defaultZone: http://${eurekahost1}:13000/eureka/,http://${eurekahost2}:13000/eureka/
      # defaultZone: http://localhost:13000/eureka/

  instance:
    # 上线后使用这个配置
#    prefer-ip-address: false
#    ip-address: 8.130.64.168
#    instance-id: ${eureka.instance.ip-address}:${server.port}
#    hostname: ${eureka.instance.ip-address}
#    lease-renewal-interval-in-seconds: 5
#    lease-expiration-duration-in-seconds: 15
    # 上线前使用这个配置
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 15
    




